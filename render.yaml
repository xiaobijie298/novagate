services:
  - type: web
    name: novagate
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: PORT
        value: "3000"
      - key: QWEN_API_KEY
        sync: false  # 不自动同步，手动在 Dashboard 设置
